// Voice AI Progress Review — scaffold only, not implemented.
// See VISION.md > "Voice AI Progress Review" and backend/services/voiceReviewService.js.

export const submitVoiceSession = async (req, res) => {
  res.status(501).json({ message: "Voice review not implemented yet — see VISION.md" });
};

export const getVoiceReviewResult = async (req, res) => {
  res.status(501).json({ message: "Voice review not implemented yet — see VISION.md" });
};

export const getVoiceReviewHistory = async (req, res) => {
  res.status(501).json({ message: "Voice review not implemented yet — see VISION.md" });
};
